/**
 * @name DSR
 * @description D => drag , S => scale , R => rotate
 * @param 
 * @option {id, width, height}
 * 
 */

class DSR {
    constructor(opt) {

    }

    add(url) {
        console.log(url)
    }
}